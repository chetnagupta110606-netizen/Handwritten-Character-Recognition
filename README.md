# Handwritten-Character-Recognition
A Handwritten Digit Recognition system built using Convolutional Neural Networks (CNN) with TensorFlow/Keras and Streamlit. The model is trained on the MNIST dataset to accurately recognize handwritten digits (0–9) from uploaded images.
